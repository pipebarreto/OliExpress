import { FormControl, InputLabel, MenuItem, Select } from "@mui/material";
import React from "react";


export function OrderBy(){

    /*const[sortBy, setSortBy]=React.useState('');

    const inputChanged = (event:any) => {
        setSortBy(event.target.value);
      };


    return(

        <FormControl variant="standard" sx={{ minWidth: 200 }}>
        <InputLabel id="demo-simple-select-filled-label">Order By</InputLabel>
        <Select
                    name='category'
                    onChange={inputChanged}
                    fullWidth
                    label="Order By"
                    variant="standard">

            <MenuItem value={'a-b'}>Name: Low to High</MenuItem>
            <MenuItem value={'b-a'}>Name: High to Low</MenuItem>
            <MenuItem value={'ab-ac'}>Price: Low to High</MenuItem>
            <MenuItem value={'ac-ab'}>Price: High to Low</MenuItem>
        </Select>
        </FormControl>

    )*/
}

